{% extends 'base.html.twig' %}

{% block title %}Hello TeamController!{% endblock %}

{% block content %}
<div class="headline">
    <h1 class="mt-4">Team</h1>
        <ol class="breadcrumb mb-4">
            <li class="breadcrumb-item"><a href="{{ path('listeteam') }}">Team</a></li>
            <li class="breadcrumb-item active">Import</li>
        </ol>

    <div class="inner">
        <p>Importation des différentes équipes grâce à un ficher externe sous Excel.</p>
    </div>
</div>

<main>
    <div class="container-fluid">
        <div class="card mb-4">
            <div class="card-header">
                <i class="fas fa-file-import mr-1"></i>
                    Team import
            </div>

            <div class="card-body">
                {{ form(my_form) }}
            </div>
        </div>
    </div>
</main>
{% endblock %}